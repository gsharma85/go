syntax = "proto3";

package sense;

message SensedData {
	string actorSystemPath = 1;
	string actorPath = 2;
	string action = 3;
	string time = 4;
}

message AckEvent {
	int32 ack = 1;
}

service SensorDataService {
	rpc senseData(stream SensedData) returns (AckEvent);
}
